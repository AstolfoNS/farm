● pom.xml中不引入新的依赖，如有pom.xml中不支持的功能需求需和老师商榷后共同决定

● Java
● JDK21
● Spring Boot
● Spring MVC
● PostgreSQL
● ORM
● Hibernate
● jQuery+EasyUI

● 项目资源应由项目所属服务器发布

特别注意

本项目中所用代码应该采用jQuery代码形式，而非js代码形式。

常见误用举例：

DOM选择

✘ let element=document.getElementById("div1");

✘ let element = document.querySelector("#div1");

✔ let element=$("#div1");

事件

✘ document.querySelector("#div1").addEventListener(...);

✔ $("#div1").on(...);

网络通信

✘ fetch(...);

✔ $.ajax(...);

循环遍历

✘ someArray.forEach(...);

✔ $.each(someArray, ... );

表单序列化

✘ let formData = new FormData(document.querySelector('#myForm'));

✔ let formData = $('#myForm').serialize();

事件绑定

✘ const button= document.querySelector("button");

button.addEventListener("click", () => {})

✔ let button= $('button')

     buttons.bind("click", (event) => {})

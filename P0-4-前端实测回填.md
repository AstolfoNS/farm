# P0-4 前端实测回填（2026-05-25）

范围：种植 / 收获 / 商店 / 地块 四块；含 WebSocket 实时推送验证。  
环境：`http://127.0.0.1:8080`（本地启动），数据库为你重建后的 `misdb`。

## 0. P0 壳子与实时链路

- [x] `GET /html/app.html` 返回 200
- [x] `GET /css/modules/farm-stage.css`、`/js/app/modules/farm.js`、`/js/app/core/ws.js` 返回 200
- [x] WebSocket 握手成功：`ws://127.0.0.1:8080/ws/server?userId=12`
- [x] 收到 `FARM_OVERVIEW` 推送（`gotFarmOverviewPush=true`）

## 1. 地块模块

- [x] `POST /gameplay/myFarmOverview` 成功（`code=200`）
- [x] `POST /gameplay/plot/unlock` 成功（`code=200`）
- [x] `POST /gameplay/plot/expand` 成功（`code=200`）
- [x] `POST /gameplay/plot/trade/page` 成功（`code=200`）

## 2. 商店模块

- [x] `POST /seed/shop/page` 成功（`code=200`）
- [x] `POST /seed/shop/home` 成功（`code=200`）
- [x] `POST /seed/shop/buy` 成功（`code=200`）
- [x] `POST /seed/shop/sell-fruit` 成功（`code=200`）
- [x] `POST /seed/shop/trade/page` 成功（`code=200`）

## 3. 种植 / 收获模块

- [x] `POST /gameplay/myPlantingPanel` 成功（`code=200`）
- [x] `POST /gameplay/seedPlantablePlots` 成功（`code=200`）
- [x] `POST /gameplay/plant` 成功（`code=200`）
- [x] `POST /gameplay/care` 成功（`code=200`）
- [x] 等待成熟后 `POST /gameplay/harvest` 成功（`code=200`）
- [x] `POST /gameplay/crop/action/page` 成功（`code=200`，本轮记录数 `3`）

## 4. 本轮回归样本

- 回归样本用户：`userId=11`，`username=p0_final_1779710480591`
- 核心链路结果：
  - `setCur/getCur/shopPage/buy/overview/unlock/expand/panel/plantables/plant/care/harvest/fruitPage/sell/shopTrades/plotTrades/cropLogs/shopHome` 全部 `code=200`

## 5. 处理的后端阻塞修复

- 修复 `SeedServiceImp.buySeed`：首次购买时若无 `user_seeds` 记录，自动创建并入库，避免 `种子库存不存在` 500。
- 修复 `CropLifecycleServiceImp.harvest`：首次收获时若无 `user_fruits` 记录，自动创建并入库，避免 `果实库存不存在` 500。

结论：P0-4 本轮已全绿。

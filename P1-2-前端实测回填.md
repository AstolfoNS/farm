# P1 第2步前端实测回填（2026-05-25）

## 本轮改动
- [x] 商店筛选：名称/品质/等级筛选条接入（`#shopSeedName/#shopSeedQuality/#shopSeedLevel`）
- [x] 商店分页联动：筛选后重置到第1页，上一页/下一页保持筛选条件
- [x] 农场操作反馈动画：地块高亮脉冲 + 浮动文案（种植/收获/养护/解锁/扩地）
- [x] 成功码兼容：前端统一兼容 `code=0` 与 `code=200`

## 四模块联调清单回填

### 1. 种植模块
- [x] `POST /gameplay/myPlantingPanel` 可返回种子背包与可种信息
- [x] `POST /gameplay/seedPlantablePlots` 可返回可种地块列表
- [x] `POST /gameplay/plant` 种植成功（实测：`userId=1, plotId=1, seedTypeId=1`）

### 2. 收获模块
- [x] 前置状态可读取（`myFarmOverview` 能识别 `harvestable/canCare`）
- [x] 当前样本作物未成熟，收获接口按预期“跳过实操”（非失败）
- [x] 农场端已接入收获成功动画与音效（待成熟后可直接触发）

### 3. 商店模块
- [x] `POST /seed/shop/home`（含 `name/seedQualityId/level/page/rows`）返回成功
- [x] `POST /seed/shop/buy` 购买成功（实测：`seedTypeId=1, quantity=1`）
- [x] `POST /seed/shop/fruit/page` 分页成功
- [x] `POST /seed/shop/trade/page` 分页成功
- [x] 购买后背包联动可见（`myPlantingPanel` 中库存已更新）

### 4. 地块模块
- [x] `POST /gameplay/myFarmOverview` 返回成功（实测：`total=6`）
- [x] `POST /gameplay/plot/status` 返回成功（实测：`locked=5, unlocked=1`）
- [x] 地块点击操作弹窗、操作后动画与刷新链路已接通

## 规范检查
- [x] 全量保持 jQuery/EasyUI 风格（无 `getElementById/querySelector/addEventListener/fetch/forEach`）
- [x] JS 语法检查通过（`node --check`）
- [x] 后端编译通过（`mvn -DskipTests compile`）

## 备注
- 本轮“收获实操”未强行改时间或改库，保持业务真实节奏；待当前作物成熟后即可在前端直接完成收获与动画验收。
